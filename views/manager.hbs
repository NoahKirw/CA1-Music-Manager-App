{{> menu id="manager"}}

<section class="ui segment">
  <h2>My Albums</h2>

    {{#each albums}}
    <div class="ui segment">
        <h3>{{name}}</h3>
        <p>Total Songs: {{tracks.length}}</p>
        <a href="/tracks/{{code}}" class="ui button">View tracks</a>
    </div>
    {{/each}} 

</section>